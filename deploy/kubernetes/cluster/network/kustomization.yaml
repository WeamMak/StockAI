apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The course-approved Calico release supplies both pod networking and
# NetworkPolicy enforcement. The immutable release tag prevents moving-tag drift.
resources:
  - https://raw.githubusercontent.com/projectcalico/calico/v3.30.2/manifests/calico.yaml
