apiVersion: v1
kind: PersistentVolume
metadata:
  name: stockai-prometheus-data
  labels:
    app.kubernetes.io/component: prometheus
    app.kubernetes.io/part-of: stockai
    stockai.io/cluster-scoped: "true"
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 5Gi
  claimRef:
    name: prometheus-data
    namespace: stockai
  csi:
    driver: ebs.csi.aws.com
    fsType: ext4
    volumeHandle: vol-placeholder-prometheus
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: topology.kubernetes.io/zone
              operator: In
              values:
                - us-east-1a
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  volumeMode: Filesystem
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
  labels:
    app.kubernetes.io/component: prometheus
    app.kubernetes.io/part-of: stockai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: stockai-prometheus-data
