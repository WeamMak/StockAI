# Implementation Status

## Current gate

- Specification: approved by the user and course staff.
- Implementation plan: approved by the user and course staff.
- Implementation authorization: explicitly provided by the user on 2026-08-02.
- Active task: T08 is implemented and verified locally on its feature branch;
  feature-branch review and the documented dev-promotion workflow remain.

## Task status

| Task | Status | Verification evidence | Remaining limitations |
|---|---|---|---|
| T01 | Complete | The architecture test first failed because all 11 planned packages were absent, then passed after the empty package structure was added. `uv lock` resolved 75 packages; `uv sync --locked` installed 73 packages with Python 3.12.13. `make lock-check`, `make format-check`, `make lint`, `make test-unit`, and `git diff --check` passed. Merged to `main` through PR #2 at `0e24de6`. | T01 intentionally contains no application behavior. Its original coverage report had zero executable application statements. |
| T02 | Complete | Red-green tests cover bounded identifiers, exact decimal money and quantity, currency, dates and UTC timestamps, bounded manager notes, revisions, environment-bound evidence, all approved case transitions, unknown/illegal states, all stable error codes, retryability, field errors, and sanitized envelopes. `make check` passed with 110 tests, strict mypy over 20 files, architecture checks, lock verification, and 92% branch-aware domain coverage. | No API, agent, MCP, persistence, or procurement policy behavior is included; those remain assigned to later tasks. |
| T03 | Complete | Red-green tests cover process liveness, lifecycle readiness, explicit unconfigured dependency status, Prometheus request/error/latency metrics with bounded labels, safe domain and request-validation envelopes, stable HTTP semantics, request-ID propagation, recursive sensitive-field redaction, protected log metadata, and correlated JSON request logs. `make check` passed with 135 tests, strict mypy over 34 files, architecture and lock checks, and 91% branch-aware coverage. A local Uvicorn process returned the expected health and metrics responses, emitted sanitized JSON logs, and shut down cleanly on Ctrl+C. | Bedrock, MCP, Odoo, DynamoDB, scans, authentication, and procurement workflows remain assigned to later tasks. `/health/dependencies` temporarily exposes only non-sensitive `not_configured` values; T14 adds its operator authentication. |
| T04 | Complete | Red-green tests cover strict environment-bound candidate inputs, bounded typed fictional output, output-schema rejection, environment mismatch, opaque bearer verification, missing/wrong credentials, safe timeout and unavailable-source retries, non-echoing errors, structured logs, and bounded Prometheus call/duration/failure/timeout/retry metrics. `make check` passed with 155 unit tests, strict mypy over 48 files, architecture and lock checks, and 91% branch-aware coverage. `make test-integration` passed 3 tests using the Python MCP client against an actual authenticated Uvicorn Streamable HTTP server; the client discovered and invoked the tool and scraped its resulting metric. Merged to `main` through PR #5 at `4759742`. | The adapter remains deterministic and fictional. Real Odoo JSON-2 contract verification and the production adapter remain T10–T11; container and deployment configuration remain later approved tasks. |
| T05 | Complete | Red-green tests cover a compiled LangGraph MCP→structured-LLM path, an approval-ready read-only result, safe MCP-timeout fallback, import boundaries, `202` manual and Cron scan creation, list/detail polling, a dedicated opaque Cron bearer, one in-process scan slot, the bounded workflow deadline, safe errors, structured logs, and bounded scan/result/LLM/token/MCP/timeout/retry metrics. `make check` passed with 163 unit tests, strict mypy over 64 files, architecture and lock checks, and 89% branch-aware coverage. `make test-integration` passed 4 tests; the new test exercised FastAPI → background scan service → compiled LangGraph → authenticated real Streamable HTTP MCP transport → deterministic fake ERP and fake structured LLM, then polled the successful result and scraped both API-side and MCP-side metrics. Merged to `main` through PR #6 at `03f4e56`. | State, locking, and tasks are process-local until DynamoDB/checkpoint work in T13. The default API composition safely reports an unconfigured workflow until T07 adds composition roots. Manual-route Cognito/RBAC/CSRF protection remains T14; the T05 Cron bearer is separate, while NetworkPolicy and secret-store delivery remain deployment tasks. The result is intentionally read-only: real Odoo, Bedrock, and draft-PO writes remain later approved tasks. |
| T06 | Complete | Tests cover overview loading, empty, populated, and safe-error states; keyboard-triggered manual scans with exact `202 Accepted` handling; bounded API response validation; scan loading, running, success, manual-review, and retryable-error states; finite polling; polling-limit behavior; and unmount cleanup. `npm test` passed 14 tests, `npm run typecheck` and `npm run lint` passed, and `npm run build` produced the Vite production bundle. Dependencies are exactly pinned in `package-lock.json`, and installation reported no known vulnerabilities. Merged to `main` through PR #7 at `fcce148`. | Cognito session, RBAC, CSRF, and idempotency protection remain T14. NGINX containerization and same-origin production proxying remain later approved container/deployment tasks. |
| T07 | Complete | Red-green integration tests start independent Uvicorn API and MCP processes and cover the complete FastAPI → compiled LangGraph → authenticated Streamable HTTP MCP path. The happy path returns the deterministic fictional read-only recommendation. The timeout path verifies three bounded attempts, retry count `2`, the safe retryable `MCP_TIMEOUT` response, sanitized structured logs, and API- and MCP-owned metrics. `make test-integration` passed all 6 tests. `make check` passed lock and format checks, Ruff, strict mypy over 71 files, 5 architecture tests, and 165 unit tests. Frontend tests passed 14 tests, type checking and linting passed, and the production build succeeded. A launcher smoke test served the frontend, accepted a scan through the Vite proxy, returned `approval_ready`, emitted the expected API/MCP/LLM events, and released its ports after Ctrl+C. Merged to `main` through PR #8 at `0c1f929`. | The walking skeleton intentionally uses deterministic local ERP and structured-LLM adapters. Real Odoo and Bedrock adapters remain T10–T12, durable state remains T13, and authentication controls remain T14. |
| T08 | Implemented and verified locally | Red-green configuration tests cover pinned multi-stage base images, non-root runtime users, fixed `stockai-api` and `stockai-mcp` entry points, service-specific health checks, explicit `/tmp` writable volumes, compiled frontend assets, no Node development server in the runtime image, same-origin `/api` and `/auth` proxying, minimal build context, and secret-material exclusions. All 5 configuration tests passed. The `stockai-api:t08`, `stockai-mcp:t08`, and `stockai-frontend:t08` images built successfully. All three ran healthy together with read-only root filesystems, `/tmp` tmpfs mounts, all capabilities dropped, `no-new-privileges`, and effective non-root UIDs. The frontend served compiled assets and proxied a manual scan through FastAPI to the authenticated real MCP transport; the scan completed `approval_ready`, and sanitized API/MCP logs recorded successful HTTP, MCP-tool, LLM, and scan events. Both fixed backend entry points also launched directly, returned their API `/health/live` and MCP `/metrics` responses, and shut down cleanly. `make check` passed lock and format checks, Ruff, strict mypy over 73 files, 5 architecture tests, and 165 unit tests. `make test-integration` passed all 6 tests. Frontend tests passed all 14 tests; type checking, linting, and the production build passed. | Feature-branch review and dev promotion remain. T09 adds Compose and the deterministic fake Odoo service. Kubernetes manifests later make the verified runtime flags declarative and add seccomp and explicit `emptyDir` mounts; real Odoo and Bedrock remain T10–T12. |

## Evidence policy

Only commands actually run successfully are recorded as passing. Generated
reports remain local under `reports/` and are not committed.
