# Implementation Status

## Current gate

- Specification: approved by the user and course staff.
- Implementation plan: approved by the user and course staff.
- Implementation authorization: explicitly provided by the user on 2026-08-02.
- T26 typed revisioned preferences are implemented and all local, clean-Odoo,
  real-transport, Kubernetes, release-contract, Argo reconciliation, and
  authenticated live dev smoke verification passes. Production promotion is
  prepared on the feature branch and remains pending PR review.
- T26A scan-detail modernization is implemented locally on
  `feature/t26a-ui`; focused frontend verification passes. Dev release and
  live responsive-browser verification remain pending.
- T24 is complete. Protected `main` promotion retained the exact four
  dev-tested image digests, and the production release records passed dev
  validation before Argo reconciliation and the production smoke check.

## Task status

| Task | Status | Verification evidence | Remaining limitations |
|---|---|---|---|
| T01 | Complete | The architecture test first failed because all 11 planned packages were absent, then passed after the empty package structure was added. `uv lock` resolved 75 packages; `uv sync --locked` installed 73 packages with Python 3.12.13. `make lock-check`, `make format-check`, `make lint`, `make test-unit`, and `git diff --check` passed. Merged to `main` through PR #2 at `0e24de6`. | T01 intentionally contains no application behavior. Its original coverage report had zero executable application statements. |
| T02 | Complete | Red-green tests cover bounded identifiers, exact decimal money and quantity, currency, dates and UTC timestamps, bounded manager notes, revisions, environment-bound evidence, all approved case transitions, unknown/illegal states, all stable error codes, retryability, field errors, and sanitized envelopes. `make check` passed with 110 tests, strict mypy over 20 files, architecture checks, lock verification, and 92% branch-aware domain coverage. | No API, agent, MCP, persistence, or procurement policy behavior is included; those remain assigned to later tasks. |
| T03 | Complete | Red-green tests cover process liveness, lifecycle readiness, explicit unconfigured dependency status, Prometheus request/error/latency metrics with bounded labels, safe domain and request-validation envelopes, stable HTTP semantics, request-ID propagation, recursive sensitive-field redaction, protected log metadata, and correlated JSON request logs. `make check` passed with 135 tests, strict mypy over 34 files, architecture and lock checks, and 91% branch-aware coverage. A local Uvicorn process returned the expected health and metrics responses, emitted sanitized JSON logs, and shut down cleanly on Ctrl+C. | Bedrock, MCP, Odoo, DynamoDB, scans, authentication, and procurement workflows remain assigned to later tasks. `/health/dependencies` temporarily exposes only non-sensitive `not_configured` values; T14 adds its operator authentication. |
| T04 | Complete | Red-green tests cover strict environment-bound candidate inputs, bounded typed fictional output, output-schema rejection, environment mismatch, opaque bearer verification, missing/wrong credentials, safe timeout and unavailable-source retries, non-echoing errors, structured logs, and bounded Prometheus call/duration/failure/timeout/retry metrics. `make check` passed with 155 unit tests, strict mypy over 48 files, architecture and lock checks, and 91% branch-aware coverage. `make test-integration` passed 3 tests using the Python MCP client against an actual authenticated Uvicorn Streamable HTTP server; the client discovered and invoked the tool and scraped its resulting metric. Merged to `main` through PR #5 at `4759742`. | The adapter remains deterministic and fictional. Real Odoo JSON-2 contract verification and the production adapter remain T10–T11B; container and deployment configuration remain later approved tasks. |
| T05 | Complete | Red-green tests cover a compiled LangGraph MCP→structured-LLM path, an approval-ready read-only result, safe MCP-timeout fallback, import boundaries, `202` manual and Cron scan creation, list/detail polling, a dedicated opaque Cron bearer, one in-process scan slot, the bounded workflow deadline, safe errors, structured logs, and bounded scan/result/LLM/token/MCP/timeout/retry metrics. `make check` passed with 163 unit tests, strict mypy over 64 files, architecture and lock checks, and 89% branch-aware coverage. `make test-integration` passed 4 tests; the new test exercised FastAPI → background scan service → compiled LangGraph → authenticated real Streamable HTTP MCP transport → deterministic fake ERP and fake structured LLM, then polled the successful result and scraped both API-side and MCP-side metrics. Merged to `main` through PR #6 at `03f4e56`. | State, locking, and tasks are process-local until DynamoDB/checkpoint work in T13. The default API composition safely reports an unconfigured workflow until T07 adds composition roots. Manual-route Cognito/RBAC/CSRF protection remains T14; the T05 Cron bearer is separate, while NetworkPolicy and secret-store delivery remain deployment tasks. The result is intentionally read-only: real Odoo, Bedrock, and draft-PO writes remain later approved tasks. |
| T06 | Complete | Tests cover overview loading, empty, populated, and safe-error states; keyboard-triggered manual scans with exact `202 Accepted` handling; bounded API response validation; scan loading, running, success, manual-review, and retryable-error states; finite polling; polling-limit behavior; and unmount cleanup. `npm test` passed 14 tests, `npm run typecheck` and `npm run lint` passed, and `npm run build` produced the Vite production bundle. Dependencies are exactly pinned in `package-lock.json`, and installation reported no known vulnerabilities. Merged to `main` through PR #7 at `fcce148`. | Cognito session, RBAC, CSRF, and idempotency protection remain T14. NGINX containerization and same-origin production proxying remain later approved container/deployment tasks. |
| T07 | Complete | Red-green integration tests start independent Uvicorn API and MCP processes and cover the complete FastAPI → compiled LangGraph → authenticated Streamable HTTP MCP path. The happy path returns the deterministic fictional read-only recommendation. The timeout path verifies three bounded attempts, retry count `2`, the safe retryable `MCP_TIMEOUT` response, sanitized structured logs, and API- and MCP-owned metrics. `make test-integration` passed all 6 tests. `make check` passed lock and format checks, Ruff, strict mypy over 71 files, 5 architecture tests, and 165 unit tests. Frontend tests passed 14 tests, type checking and linting passed, and the production build succeeded. A launcher smoke test served the frontend, accepted a scan through the Vite proxy, returned `approval_ready`, emitted the expected API/MCP/LLM events, and released its ports after Ctrl+C. Merged to `main` through PR #8 at `0c1f929`. | The walking skeleton intentionally uses deterministic local ERP and structured-LLM adapters. Real Odoo and Bedrock adapters remain T10–T12, durable state remains T13, and authentication controls remain T14. |
| T08 | Complete | Red-green configuration tests cover pinned multi-stage base images, non-root runtime users, fixed `stockai-api` and `stockai-mcp` entry points, service-specific health checks, explicit `/tmp` writable volumes, compiled frontend assets, no Node development server in the runtime image, same-origin `/api` and `/auth` proxying, minimal build context, and secret-material exclusions. All 5 configuration tests passed. The `stockai-api:t08`, `stockai-mcp:t08`, and `stockai-frontend:t08` images built successfully. All three ran healthy together with read-only root filesystems, `/tmp` tmpfs mounts, all capabilities dropped, `no-new-privileges`, and effective non-root UIDs. The frontend served compiled assets and proxied a manual scan through FastAPI to the authenticated real MCP transport; the scan completed `approval_ready`, and sanitized API/MCP logs recorded successful HTTP, MCP-tool, LLM, and scan events. Both fixed backend entry points also launched directly, returned their API `/health/live` and MCP `/metrics` responses, and shut down cleanly. `make check` passed lock and format checks, Ruff, strict mypy over 73 files, 5 architecture tests, and 165 unit tests. `make test-integration` passed all 6 tests. Frontend tests passed all 14 tests; type checking, linting, and the production build passed. Merged to `main` through PR #9 at `9218119`. | Kubernetes manifests later make the verified runtime flags declarative and add seccomp and explicit `emptyDir` mounts; real Odoo and Bedrock remain T10–T12. |
| T09 | Complete | The Compose topology test first failed because the approved files did not exist, then passed after the four-service stack was defined. `make compose-validate` rendered both configurations successfully. `make test-e2e` passed all 5 tests in 134.15 seconds: the bounded topology plus happy path, no-valid-response, malformed fake Odoo response, and service-timeout scenarios. Each behavior test entered through the compiled frontend's same-origin proxy and crossed FastAPI, compiled LangGraph, authenticated real Streamable HTTP MCP transport, and the separate deterministic fake Odoo HTTP process; cleanup and fictional-token log checks ran for every isolated project. The stack has two explicit networks, health-gated dependencies, CPU and memory limits, read-only root filesystems, dropped capabilities, `no-new-privileges`, and disposable `/tmp` mounts. `make check` passed lock/format checks, Ruff, strict mypy over 76 files, 5 architecture tests, and 165 unit tests. `make test-integration` passed all 6 existing real-transport tests. Frontend tests passed all 14 tests; type checking, linting, and the production build passed. Merged to `main` through PR #10 at `b6ca15a`. | The fake Odoo endpoint is explicitly an internal test contract, not an invented Odoo 19 JSON-2 contract. Real Odoo contract verification reached the approved T10 stop condition; real Bedrock and durable state remain T12–T13. |
| T10 | Complete | Red-green tests first exposed PostgreSQL initialization capability needs, internal-only port suppression, missing analytic ACL membership, the removed `uom_po_id` field, and default purchase-tax behavior. The final `make odoo-contract` run passed all 9 tests in 55.53 seconds and wrote JUnit XML. One fresh pinned Odoo/PostgreSQL stack verified immutable images, network/port isolation, clean module installation, idempotent ORM user/key bootstrap, mode-`0600` key storage, safe JSON-2 errors, auth/database/version behavior, `/doc` and `fields_get`, all required standard fields/methods, fictional reordering/supplier/analytic records, nested PO creation, analytic distribution, `write_date`, confirmation, partial receipt/backorder, linked return, cancellation/reset, denied configuration mutations, and the missing budget/non-atomic standard-action contracts. Teardown removed the key, database, containers, networks, and volumes; Ruff and strict mypy passed. Merged to `main` through PR #11 at `520def7`. | The standard Odoo limitations found by T10 are addressed by T11A's project add-on. The real Odoo adapter remains T11B. |
| T11A | Complete | Red-green static contracts cover the pinned derived Odoo image, upstream entrypoint retention, non-root runtime, exact add-on/bootstrap copy surface, locked `boto3`, approved manifest dependencies, and secret exclusions. The first Docker run exposed three Odoo 19 contract details: public `fields_get` omits the internal `tracking` attribute, JSON-2 maps business validation failures to HTTP `422`, and the API-key table's `index` column is intentionally absent from the ORM model. Tests now verify actual chatter tracking and the real status contract, while bootstrap reconciliation relies on one dedicated active named key plus credential verification. The final clean `make odoo-contract` run passed all 19 tests in 166.84 seconds and wrote JUnit XML. `make check` also passed Ruff, strict mypy over 82 files, 5 architecture tests, and all 165 unit tests. Merged to `main` through PR #12 at `5a54a27`. | The real candidate-read adapter was implemented in T11B; the remaining Odoo MCP operations remain assigned to later vertical slices. |
| T11B | Complete | Red-green tests cover fixed JSON-2 operations and paths, bearer/database headers, the 10-second timeout, two bounded transient retries, permanent-error handling, response limits, safe errors, and strict rejection of malformed or cross-company Odoo data. The MCP composition root selects the real adapter only in explicit `odoo` mode and retains deterministic adapters for existing tests. `make check` passed Ruff, strict mypy over 89 source files, 5 architecture tests, and all 188 unit tests. `make test-integration` passed all 9 tests in 150.38 seconds. `make odoo-contract` passed all 20 tests in 237.30 seconds and wrote JUnit XML. The added integration test seeded real Odoo, called the candidate tool over authenticated MCP Streamable HTTP and JSON-2, completed the existing LangGraph scan, verified bounded Odoo/MCP metrics, and found no key or database password in captured logs. Merged to `main` through PR #13 at `b69691f`. | T11B intentionally implements only the approved replenishment-candidate read; later tasks own the other MCP operations and production secret delivery. |
| T12 | Complete | Red-green tests cover the fixed `openai.gpt-oss-20b-1:0` model and `us-east-1` region, disabled hidden SDK retries, a 30-second per-attempt maximum, two bounded transient retries with exponential backoff/jitter, one schema repair, safe unavailable/invalid fallbacks, strict eligible-identifier and field validation, rejection of model-supplied arithmetic, mandatory budget acknowledgement, bounded text, injection-like business data, separate input/output token extraction, and suppression of reasoning-content blocks. The API composition root now validates explicit `local\|bedrock` selection: local mode never constructs an AWS client, while Bedrock mode wires the approved client, prompt, schema, and validator through API scan creation, LangGraph, polling, safe success/failure results, and aggregate LLM metrics using a mocked provider. `make check` passed lock/format checks, Ruff, strict mypy over 97 source files, 5 architecture tests, and all 208 unit tests; the 20 focused T12 tests and 3 focused real-transport integration regressions passed, and all Compose files rendered successfully. A production wheel build confirmed that the Markdown prompt is packaged. Merged to `main` through PR #14 at `b984960`. | The first live Bedrock call remains explicitly deferred to the T23 dev smoke test. Local scripts and Compose pin deterministic local mode; later deployment configuration selects Bedrock. T28 completes full offer/preference reasoning and final comparison-fallback metrics. |
| T12A | Complete | The Makefile contract first failed because `make lint` omitted the existing frontend command; the target now runs the pinned ESLint script. The derived Odoo image consumes a committed seven-distribution Python 3.12 closure with exact versions and SHA-256 hashes through pip `--require-hashes`. A first clean build exposed Debian's record-less `urllib3`; `--ignore-installed` now installs the locked closure into `/usr/local` without mutating Debian packages. Eleven mocked sink tests cover exact-ARN validation, ARN-derived region selection, exact `GetSecretValue`/`PutSecretValue` identifiers, missing and malformed values, and non-disclosure. The real-Odoo bootstrap contract runs both distinct fictional `dev` and `prod` seeds twice. `make check` passed Ruff, strict mypy over 100 source files, ESLint, 5 architecture tests, and all 219 unit tests. `make odoo-contract` rebuilt the image and passed all 23 tests in 399.82 seconds with JUnit output. Merged to `main` through PR #16 at `abe6816`. | No live AWS call was made. T17 provisions the exact secret and temporary write policy, T19B runs the environment bootstrap Jobs, T21 adds `actionlint` when workflows exist, and T32 validates live rotation and policy detachment. |
| T13 | Complete | Red-green tests cover immutable sanitized audit events; environment-prefixed application keys; atomic conditional case/idempotency creation; idempotent replay and conflicting reuse; optimistic revisions; strongly consistent approval reads; append-only audit writes; TTL fields; opaque pagination; separate 30-day dev and one-year prod checkpoint retention; explicit `memory\|dynamodb` API composition; stable case-based LangGraph `thread_id`; and exclusion of transient Odoo candidates and raw LLM response objects from checkpoints. `make check` passed lock/format checks, Ruff, strict mypy over 108 files, ESLint, 5 architecture tests, and all 242 unit tests. Eight relevant real-process integrations passed in 21.96 seconds. The two T13 integrations used pinned DynamoDB Local 3.3.0 to prove conditional duplicate prevention and that a completed scan plus sanitized graph checkpoint remain readable after replacing the API process. Merged to `main` through PR #17 at `cca7208`. | The default local mode intentionally remains in-memory. T17 provisions encrypted environment tables, TTL/PITR, and IAM; T19B/T23 select deployed DynamoDB and validate the first live AWS path. Later case slices add their richer application records without duplicating Odoo master data. |
| T14 | Complete | Red-green tests cover authorization-code PKCE, one-use state, nonce and ID-token verification, exact Cognito role groups, safe callback errors, secure opaque cookies, session rotation/expiry/logout, session-bound CSRF, officer/manager route protection, disabled self-signup assumptions, and idempotent fictional user/group bootstrap without credential output. Runtime configuration accepts only fail-closed `disabled` or real `cognito`; the local identity exists only as a Python test injection. `make check` passed lock/format checks, Ruff, strict mypy over 123 files, ESLint, 5 architecture tests, and all 258 unit tests. All 17 frontend tests plus typecheck/lint/build passed; the bundle secret scan passed. Eight real-process integrations passed, including DynamoDB Local persistence of a token-free opaque session across API replacement. All 5 Compose checks passed across the topology and four deterministic scenarios. Merged to `main` through PR #18 at `fd6ba1d`. | No live Cognito/AWS call was made. T17 provisions separate dev/prod pools, clients, tables, secrets, and IAM; T19B bootstraps the fictional users; T23/T24 perform real Cognito dev/prod smoke checks. The base runtime grants no local identity, while local scripts and tests use an unconfigurable test-only adapter. |
| T15 | Complete; merged to `main` through PR #19 at `f89a089` | Eight red-green static contracts cover dedicated encrypted/versioned/private S3 state, encrypted on-demand DynamoDB locking with retention protection, exact immutable GitHub pull-request and protected-environment OIDC subjects, state-scoped plan/apply role permissions, parameterized account/repository/CIDR/state inputs, and a CLI-only runbook. Terraform `1.15.8` formatting, initialization with locked AWS provider `6.58.0`, and provider-schema validation passed. `make check` passed Ruff, strict mypy over 125 files, ESLint, 5 architecture tests, and all 258 unit tests; `git diff --check` passed. | No account-specific tfvars or credentials were supplied. The real plan, apply, and post-apply AWS verification remain behind the separate infrastructure approval gate; no live AWS call was made. The OIDC roles intentionally contain only remote-state permissions until later reviewed infrastructure tasks add resource-scoped workload permissions. The approved course plan's DynamoDB locking remains supported but is deprecated upstream in favor of S3 lockfiles. |
| T16 | Complete; merged to `main` through PR #20 at `debbb5f` | The Terraform-plan test first failed because the platform root was absent. Twelve final plan contracts now parse `terraform show -json` and cover the one-control-plane/two-ASG topology, exact active `1/1/3` and inactive `0/0/3` capacities, invalid-capacity rejection, isolated temporary environment overrides, two public AZs, Internet Gateway routing, absence of NAT/EKS/scaling policies, fixed `t3.medium` nodes, encrypted 30 GiB `gp3` roots, numbered launch-template refreshes, single-subnet environment placement, three separate SSM-only node roles/profiles, restricted public ingress, shared-account `weam-stockai-` names and `Owner = weam` tags, and all 11 approved outputs. Terraform `1.15.8` formatting and provider-schema validation passed with locked AWS provider `6.58.0`; all 20 infrastructure tests passed. `make check` passed lock/format checks, Ruff, strict mypy over 127 source files, ESLint, 5 architecture tests, and all 258 unit tests. | The deterministic plan uses fake credentials, disables AWS account calls only in an isolated temporary configuration copy, and does not contact or mutate AWS. Account-specific remote-backend initialization, a reviewed real plan, quota/cost confirmation, apply, and post-apply verification remain behind the explicit infrastructure approval gate. T18A still owns kubeadm installation and worker join. |
| T17 | Complete; merged to `main` through PR #21 at `c22a8cd` | Fourteen red-green plan tests cover separate encrypted DynamoDB tables, Secrets Manager entries, Cognito pools/clients/groups, environment-scoped Loki prefixes and IAM, the exact Bedrock model ARN, read-only normal worker policies, the finite exact-secret Odoo bootstrap write variant, six encrypted 5 GiB `gp3` data volumes in matching environment AZs, seven daily prod Odoo/PostgreSQL snapshots, one six-host ALB/ACM/Route 53 edge, ASG target attachments, ALB-only NodePort ingress, S3 privacy/encryption/versioning/retention, $70/$90 budgets, required outputs, and excluded services. Terraform `1.15.8` formatting and provider-schema validation passed for all three roots with locked AWS provider `6.58.0`; all 34 infrastructure tests passed. `make check` passed lock/format checks, Ruff, strict mypy over 129 source files, ESLint, 5 architecture tests, and all 258 unit tests. | The deterministic plan tests use fake credentials, disable AWS account calls only in isolated temporary configuration copies, and do not contact or mutate AWS. Account-specific remote-backend initialization, reviewed real plans, quota/cost confirmation, apply, and post-apply verification remain behind the explicit infrastructure approval gate. T18C installs NGINX Ingress before target health can be accepted; later tasks mount and recover the retained volumes and configure the deployed workloads. |
| T18A | Complete; live replacement acceptance passed | Red-green contracts cover the encrypted runtime-owned SSM join parameter, exact control-plane write and worker read IAM, no EKS policy, finite 24-hour tokens on a 12-hour systemd timer, strict seven-argument join validation without `eval`, bounded SSM/join retries and timeouts, EC2 private-DNS node names, hard dev/prod labels and taints, pinned Kubernetes/containerd/runc/CNI/Calico versions, restricted kubeconfig, compressed control-plane and launch-template user data, and the pinned Calico render. The final 8 focused tests and all 42 infrastructure tests passed. ShellCheck `0.10.0`, Bash syntax, Terraform `1.15.8` formatting/provider-schema validation, and `make check` passed; `make check` covered Ruff, strict mypy over 130 source files, ESLint, 5 architecture tests, and all 258 unit tests. The reviewed saved plan applied with 0 additions, 4 in-place worker changes, and 0 destructions; both worker ASG refreshes reached `Successful` at 100%. The control plane and dev/prod workers reached Kubernetes `Ready` with private-DNS names, exact environment labels/taints and roles, matching subnets/AZs, and healthy Calico. A second controlled dev ASG termination preserved desired capacity and created `i-021710ba1ad1ab765` as `ip-10-0-1-59.ec2.internal`; it independently completed cloud-init and joined Ready with only the dev scheduling identity, dev role, dev subnet/AZ, and a `1/1 Running` Calico pod. User-data, cloud-init, Terraform-output, and credential-context journal checks exposed no bootstrap credential; broad journal-pattern matches were verified as Kubernetes Event names and container image digests. | Live T18B preflight confirmed every kubeadm Node has an empty `.spec.providerID`. The old `ip-10-0-1-207.ec2.internal` record was manually deleted after its dev ASG instance was confirmed terminated; the control plane and current dev/prod workers remained Ready. |
| T18B | Complete; normal live acceptance passed | Live preflight proved that all cluster Nodes have empty `.spec.providerID`, so the approved correction binds the EventBridge instance and ASG identity to EC2 private DNS/private IPv4 plus Kubernetes Node `InternalIP` and environment instead. The first 2026-08-11 dev drill proved the cleanup command itself succeeded but exposed an SSM read-after-write visibility race; a regression test reproduced it before the bounded `InvocationDoesNotExist` retry fix. After the user reviewed and applied the new saved plan, the second dev drill terminated `i-02ec53c048a80bb6f` without reducing desired capacity. Lambda recorded `outcome=clean`, `ssm_status=Success`, `heartbeat_count=1`, no error, and old Node `ip-10-0-1-65.ec2.internal`; the old Node disappeared. The ASG returned healthy and InService with `i-09a0b411b577f4208`, and Kubernetes returned to three Ready Nodes with replacement `ip-10-0-1-194.ec2.internal` carrying exactly `stockai.io/environment=dev` and the dev `NoSchedule` taint. All 15 Lambda tests, 16 deterministic lifecycle/platform plan tests, Terraform validation/formatting, `git diff --check`, and `make check` passed; `make check` covered Ruff, strict mypy over 132 source files, ESLint, 5 architecture tests, and all 258 unit tests. | On 2026-08-11 the user explicitly deferred the live unavailable-control-plane/SSM drill and accepted T18B as finished. Automated tests verify bounded timeout, non-clean `CONTINUE`, and the hook's 300-second default `CONTINUE`; the live failure scenario remains unexecuted. |
| T18C | Complete; live acceptance passed | Seven red-green render tests cover immutable controller pins, NGINX as a worker-only DaemonSet on fixed NodePort `32080`, static-only EBS CSI placement and bounded RBAC, the Metrics API and kube-state-metrics, Argo CD control-plane placement, namespace isolation, no cert-manager, and no business-workload control-plane toleration. A Terraform plan assertion covers the tag- and resource-scoped control-plane EBS attach/detach policy. Metrics-server reproduced a deterministic kubelet scrape failure because the kubeadm serving certificates lack IP SANs; a new render assertion failed before the approved targeted patch added exactly one `--kubelet-insecure-tls` argument. All seven controller tests, Kubernetes `1.35.5` built-in schema validation, and `make check` passed; `make check` covered strict mypy over 134 source files and all 258 unit tests. Live acceptance then confirmed three Ready nodes, resource metrics for every node, ready EBS CSI and Argo CD controllers, and exactly two Ready worker-only NGINX pods. The reviewed edge plan applied with 37 additions, 0 changes, and 0 destructions; ACM reached `ISSUED`, and the existing dev and prod ASG workers both became healthy in their isolated ALB target groups. One terminated worker had left a stale `NotReady` Node record; EC2 lookup confirmed the instance was absent, the Node was removed, and its orphaned DaemonSet pods disappeared. | T32 must review the bounded kubelet server-identity risk introduced by `--kubelet-insecure-tls`. Public application routes remain intentionally unavailable until later environment workload and Ingress tasks. |
| T19A | Complete; local verification passed | Red-green render tests cover three exact 5 GiB static EBS CSI PV/PVC bindings per environment, `ReadWriteOnce`, `Retain`, empty StorageClass selection, AZ rather than hostname affinity, exact environment claim prebinding, no Grafana PVC, isolated namespaces/configuration/hosts/seed profiles/secret references, token-free service accounts, restricted Pod Security labels, default-deny ingress and egress, and the fixed three-claim/15 GiB storage budget. The bounded synchronization CLI accepts only the approved dev/prod JSON shape, changes only six volume handles and two AZ values, rejects missing/extra environments or workloads without mutation, and does not echo rejected secret-like input. All 11 focused tests and the complete 18-test Kubernetes suite passed. Kubeconform `0.7.0` strict validation against Kubernetes `1.35.0` schemas reported 18 valid standard resources, 0 invalid resources, and 0 errors per overlay; seven External Secrets CRD resources were intentionally skipped. `make check` passed lock/format checks, Ruff, strict mypy over 135 source files, ESLint, 5 architecture tests, and all 258 unit tests. | No workload or AWS resource was deployed or changed. A read-only EC2 inventory found no Terraform-managed application data volumes, so the committed overlays retain the approved sample volume coordinates. Before deployment, apply/review the T17 environment roots, feed their non-secret volume IDs/AZs through `sync_terraform_outputs.py`, and validate the External Secrets `v1` resources once the pinned T20A controller/CRDs exist. |
| T19B | Complete; merged to `main` through PR #27 at `055aa01` | Red-green render contracts cover five one-replica Deployments, the finite idempotent Odoo bootstrap Job, the daily non-overlapping scan CronJob, five private ClusterIP Services, the two approved public host routes, explicit liveness/readiness/startup probes, termination bounds, restricted security contexts, exact retained-volume mounts, three `1..3` CPU HPAs at 50%, environment node label/taint placement, and documented default-deny allows including narrow EC2 metadata access for worker-role credentials. Odoo and its Job share one digest reference; the CronJob reuses the API digest; dev and prod share the complete project-image map. Steady application requests total 675 millicores and 1504 MiB per environment before HPA expansion. The 13 focused tests and complete 31-test Kubernetes suite passed. Kubeconform `0.7.0` strict validation against Kubernetes `1.35.0` schemas reported 84 valid standard resources, 0 invalid resources, and 0 errors across both overlays; 14 External Secrets CRD resources were intentionally skipped. `make check` passed lock/format checks, Ruff, strict mypy over 136 source files, ESLint, 5 architecture tests, and all 258 unit tests. | No workload, image, or AWS resource was deployed or changed. T22 must replace the four explicit sentinel project-image digests with published immutable Docker Hub digests before Argo reconciliation. The reviewed T17 apply/output synchronization must also replace the non-secret Cognito coordinates and exact Odoo bootstrap secret ARN; bootstrap still requires the approved temporary exact-secret worker policy attachment and follow-up detachment. |
| T20A | Complete locally; verification passed | Dev and prod render separate one-replica Prometheus, Grafana, Loki, Alertmanager, blackbox exporter, namespace/controller-class-scoped External Secrets, and namespace-filtered Fluent Bit collectors. Prometheus uses the retained 5 GiB claim with 7-day/4 GB bounds; Grafana uses only a 256 MiB `emptyDir` and Git-provisioned Prometheus, Loki, and SDK-default CloudWatch data sources; Loki uses only its environment S3 prefix with bounded 1 GiB WAL/cache and environment retention; blackbox targets are limited to the three approved HTTPS hosts; only Grafana joins frontend/Odoo on public ingress. Restricted application namespaces remain unchanged, while the read-only host-log collectors use dedicated baseline namespaces. Observability requests are capped at 300 millicores and 768 MiB per environment. Forty environment render tests, eight cluster-controller/CRD tests, and nine focused Terraform bootstrap tests passed. Kubeconform `0.7.0` strict validation against Kubernetes `1.35.0` reported 76 valid standard resources, 0 invalid resources, and 0 errors per overlay; seven External Secrets CRD resources per overlay were intentionally skipped. `make check` passed lock/format checks, Ruff, strict mypy over 137 source files, ESLint, five architecture tests, and all 258 unit tests. | No workload, image, Terraform plan, or AWS resource was deployed or changed. Before reconciliation, replace the Loki bucket placeholder and T19/T22 deployment sentinels with reviewed Terraform outputs and immutable published image digests. Live acceptance must verify Prometheus history after pod replacement, Grafana reconstruction, one application scrape, sanitized Fluent Bit → Loki → S3 delivery, and public HTTPS/certificate probes. The worker launch-template IMDSv2 hop-limit change requires a reviewed Terraform plan/apply and rolling instance refresh. T20B owns the operational dashboard panels and actionable alert rules. |
| T20B | Complete locally; verification passed | Four Git-managed Grafana dashboards cover agent health, LLM/MCP, Kubernetes/capacity, and dependencies/edge using bounded Prometheus and CloudWatch queries. Twelve Prometheus alerts carry severity, operator ownership, evidence guidance, and exact runbook anchors, and remain routed only to the internal Alertmanager. Six focused T20B tests and the complete 54-test Kubernetes suite passed. Promtool from the pinned Prometheus `v3.5.0` image validated all 12 rules. A clean pinned Grafana `12.1.0` container became healthy and loaded exactly the four dashboards into the provisioned StockAI folder. Kubeconform `0.7.0` strict validation against Kubernetes `1.35.0` schemas reported 78 valid standard resources, 0 invalid resources, and 0 errors per overlay; seven External Secrets CRD resources per overlay were intentionally skipped. Repository format, Ruff, strict mypy, frontend lint, architecture, and unit checks passed. | No workload or AWS resource was deployed or changed. Deployment-time acceptance must fire one safe dev test alert from each application, Kubernetes, and dependency group, follow the runbook, and verify internal Alertmanager/Grafana visibility. CloudWatch-owned ALB/Lambda/lifecycle signals require live AWS data; the Odoo-key-expiry alert becomes active when the planned rotation metric is exposed. |
| T21 | Complete; merged to `main` through PR #30 at `5be9da7` | A versioned JSON schema and two bounded Python CLIs create and verify deterministic, tamper-evident release metadata for the source commit/tree, exact frontend/API/MCP/StockAI-Odoo digest maps, per-image provenance, Docker Scout result, dev validation evidence, and creation time. The pull-request workflows passed after setup-uv pinning, fictional Compose configuration, exact Gitleaks baselining, informational Docker Scout behavior, formatting, uv invocation, and ShellCheck corrections. | No AWS apply, image publication, or workload deployment was performed. The workflow retains state-only OIDC roles and never grants administrator access. T21A automates the non-secret GitHub configuration and root inputs; reviewed resource-scoped apply-role permissions remain required before GitHub-hosted workload applies. |
| T21A | Implementation and approved Budget removal complete; final branch verification pending | Guided discovery/input-generation, output synchronization, GitHub configuration, saved-plan orchestration, workflow replacement of four JSON variables, and email-backed AWS Budget removal are implemented locally. Strict typing over 144 files, 29 focused tests, all 63 Terraform contracts, all 54 Kubernetes tests, Terraform formatting, actionlint, and diff checks pass. Against account `228281126655`, the user-approved saved edge plan with SHA-256 `7e546cf0d47ef7961b0d0be6dad472ba62faa81bd3eb664e25688268e268d7b7` applied exactly `0 added, 0 changed, 2 destroyed`; both were the email-backed Budget resources. A post-apply refresh plan reported `No changes`. | GitHub CLI is not installed in this workspace, so current-repository variable/environment synchronization was not exercised live. Existing state-only OIDC roles still need reviewed resource-scoped workload permissions before GitHub-hosted workload applies. |
| T21B | Implemented; approved broad-IAM amendment verified offline; live lifecycle verification pending | Test-first implementation retains manual-only protected provision/destroy workflows, sequential saved plans and checksums, non-interactive runner/output operations, and bounded redacting SSM installation/quiescence. The approved amendment replaces the accumulated per-service lifecycle/discovery policies with AWS-managed `ReadOnlyAccess` on the plan role and protected `AdministratorAccess` on the apply role. Scoped state/locking policies remain, and a dedicated explicit-deny policy protects direct mutation of the state bucket and objects, lock table, GitHub OIDC provider, bootstrap roles, and bootstrap policies. Focused T21B checks passed (25), all Terraform roots validated, all infrastructure contracts passed (67), and `make check` passed 296 Python and 17 React tests plus formatting, lint, typing, architecture, and actionlint. | No live plan or apply was run for this amendment. Docker-dependent integration/e2e checks and kubectl-dependent Kubernetes checks could not run in this WSL environment because Docker and kubectl are unavailable; CI supplies those dependencies. Generate and review a fresh local bootstrap saved plan, apply it only after separate explicit approval, then start a completely new protected provision run. The account-wide apply-role blast radius is accepted only for this single-project course account and must be replaced with least privilege before production reuse. The separately approved destruction drill remains pending and environment data volumes remain intentional destructive targets. |
| T22 | Complete; live dev reconciliation accepted | The changed-image workflow, immutable digest desired state, declarative Argo tracking, report-only Scout behavior, and local-only `make promote-dev` behavior remain unchanged. After the protected provision rerun installed the External Secrets CRDs, live evidence exposed three independent initial-reconciliation problems: External Secrets pods timed out reaching the Kubernetes API, Loki rejected retention without a delete-request store, and three never-mounted dev PVs retained immutable placeholder handles. Red-green repair contracts now require control-plane TCP 6443 ingress from exactly the fixed Calico pod CIDR `192.168.0.0/16`, Loki `delete_request_store: s3`, and a bounded operator runbook that preserves `Retain`, names only the three dev storage sets, and forbids destructive AWS, namespace, force, and production operations. On 2026-08-14, the full platform plan suite passed 14 tests; observability plus runbook suites passed 19 tests; `make terraform-validate` passed all 68 infrastructure tests after moving temporary provider copies off the 7.7 GiB `/tmp`; and `make kubernetes-validate` passed 55 tests plus strict Kubeconform validation of both 85-resource overlays with 0 invalid resources and 0 errors. The first Kubernetes validation attempt had 47 passes and 8 network errors because sandbox DNS blocked the pinned Calico URL; the network-enabled rerun passed. Live acceptance subsequently confirmed all six ExternalSecrets ready, every application Deployment available, the finite Odoo bootstrap completed, MCP ready, Fluent Bit `1/1 Running`, the temporary Odoo-key IAM policy detached, and Argo CD `Synced` and `Healthy`. | T24 has not started. |
| T23 | Complete; exact dev smoke and worker replacement passed | On 2026-08-14, `make smoke-dev` passed against release `sha256:79ad0468c9051bc1fec4e3bcfb1dcb5afb9a97610a77a1072e8895d160ffe44b` and appended immutable validation evidence for smoke run `dev-smoke-20260814T171215Z`. The approved representative drill terminated dev worker `i-08909b360c03d59dc`; lifecycle cleanup recorded `outcome=clean`, two bounded heartbeats, and successful SSM cleanup. The old Node disappeared and ASG replacement `i-066d3a21753ac4937` joined Ready as `ip-10-0-1-129.ec2.internal` with the exact dev label, taint, subnet/AZ, and worker role. All three retained EBS volumes reattached automatically without force-detach; Odoo/PostgreSQL fictional data and the pre-replacement Prometheus sample remained; every dev workload, the ALB target, public HTTPS, Argo, and all four Git-provisioned Grafana dashboards recovered healthy. Sanitized local worker evidence digest: `sha256:8646941b24b4b86c179915365afe30798c47dcded246b62d044a3f95108701a2`. | The reusable worker drill need not be repeated unless worker/storage behavior changes or this evidence becomes invalid. T24 has not started. |
| T24 | Complete; protected production promotion passed | PR #47 merged the exact dev-to-prod promotion into protected `main` at `51d504e`; follow-up PRs #48 and #49 completed fresh-cluster Argo bootstrap and bounded Argo observation at `3619b78`. The production release records passed dev evidence for `sha256:f48966db0d45a9fb0577f881924c8cef35a579b636b72a06129fb1d86a22a075` and preserves the dev-tested API, frontend, MCP, and Odoo digest map byte-for-byte. The main workflow verified the release without rebuilding or retagging, observed Argo reconciliation, and completed the public production smoke. | T24 changed deployment/release orchestration only. T25 must publish and validate a new four-image release after its application behavior is complete. |
| T25 | Complete; seed-automation hotfix passed dev validation | The typed, environment-bound `ProcurementEvidence` boundary now preserves the exact 15-point 14-day inventory timeline, reorder trigger and need-by date, full/partial/residual existing coverage, deterministic eligible/rejected offers, configured currency and UoM conversion, MOQ rounding, normalized cost and excess, 365-day completed-order/on-time/limited-history evidence, and authoritative calendar-month budget/overage. LangGraph gathers at most 50 candidates with at most three concurrent evidence calls before LLM reasoning; skipped evidence cannot reach the LLM. The strict read-only MCP tool, Odoo JSON-2 adapter, DynamoDB case persistence, authenticated case/scan API, audit snapshots, bounded logs/metrics, fictional seed, and React evidence/skip view carry the same record. `make ACTIONLINT=/tmp/actionlint check` passed strict mypy over 171 source files, Ruff/ESLint/actionlint/architecture checks, 358 Python unit tests, and 17 React tests. The final `make test-integration` passed all 12 real-transport tests in 168.96 seconds. The final clean `make odoo-contract` passed all 23 image, JSON-2, add-on, idempotent seed, real adapter, MCP, and LangGraph tests in 216.46 seconds. `make kubernetes-validate` passed 56 tests and strict Kubeconform validation of both 85-resource overlays with 0 invalid resources and 0 errors; 65 release/workflow regression tests and `git diff --check` passed. Live validation also reconciled stale fictional demand dates and draft quantities in the idempotent seed, and added `ClientIP` affinity so HPA-scaled Streamable HTTP MCP sessions remain on one replica; the focused Odoo packaging checks and all 13 application-overlay tests passed. `make smoke-dev` then passed release `sha256:1668d6e6bc16fb8edf5b63ace10514e3dc79cbc711318241ae3f4d2b8a2e2fca` at Argo revision `6d97672dac75d2f4f0c316642d650a84ff0be09f`, smoke run `dev-smoke-20260815T120434Z`, with sanitized evidence digest `sha256:f91567c10ccb1d0c5dc774b31d955e4bc6f215ac55f3da09d37d6528e01a81b8`. The smoke verified HTTPS, Cognito session/CSRF, FastAPI, LangGraph, Bedrock, authenticated Streamable HTTP MCP, real seeded Odoo, DynamoDB, Prometheus, and sanitized Loki evidence. The first production smoke authenticated but failed with `NO_VALID_OFFER`, proving prod had not received the refreshed T25 seed. The hotfix packages the idempotent seed and verifier in the Odoo image and adds a bounded Argo-managed seed Job for both environments. Its 37 focused workflow/image/release/render tests passed, and `make kubernetes-validate` passed all 56 tests plus strict Kubeconform validation of both 86-resource overlays with 0 invalid resources and 0 errors. The live hotfix smoke passed release `sha256:09e2c3f6fb48c541eff5ce577c9f36f0a44af5fff68225c8f987d86c8db56c79` at Argo revision `d975487bbaab1faada93feb9ee4fd61a1e47f343`, smoke run `dev-smoke-20260815T140422Z`, with sanitized evidence digest `sha256:abbb61bbec01334f1ba752fa27fd009b9be79460e604883b4fc92fe59ab27127`. | T25 remains read-only: typed revisioned preferences begin in T26, draft PO creation begins in T28, and manager approval/confirmation begins in T29. Production hotfix validation must pass before T26 starts. |
| T26 | Complete in dev; authenticated smoke passed; production promotion pending | Odoo now stores one current typed profile per company/category/product scope with ordered price/delivery/reliability criteria, bounded premium policy, advisory/hard enforcement, tracked administrator changes, archive-only lifecycle, and row-locked monotonic revisions. The read-only adapter and third MCP tool resolve product → category → company; LangGraph applies policy before model reasoning and snapshots the exact profile, revision, and results through evidence, persistence, API/audit output, React, metrics, and Grafana. `make ACTIONLINT=/tmp/actionlint check` passed strict mypy over 177 source files, Ruff/ESLint/actionlint/architecture checks, 370 Python unit tests, and 17 React tests. `make test-integration` passed all 12 real-transport tests; the clean Odoo contract passed all 24 tests; and Kubernetes validation passed 56 tests plus both 86-resource overlays with 0 invalid resources or errors. Live reconciliation exposed and repaired two upgrade boundaries: the seed Job now upgrades an already-installed Odoo add-on before seeding, and the evidence reader accepts only the exact preference-free T25 schema while retaining strict T26 validation. Dev release `sha256:3a484d89fb6fab209aa2a38acfeb04f4ba579f4c7a62dda55331b16816a3d69a` reconciled `Synced` and `Healthy` at `59afb5003115e95ba5863a8aeed57a249db71e6a`. Authenticated `make smoke-dev` passed as `dev-smoke-20260815T161508Z` with sanitized evidence digest `sha256:b4b028f5bbe5c8138af4145f6f55cf078e63a1ef8cb9ed1d94713f20962d247a`, verifying the exact four deployed image digests and the public critical path. | Production desired state is generated from the exact dev-validated release and remains pending PR checks, merge, Argo reconciliation, and production smoke. T26 remains read-only; draft PO creation starts in T28. |
| T26A | Implemented locally; frontend verification passed | The scan detail now uses a responsive executive-summary foundation with need-by date, residual need, eligible-offer count, proposed budget impact, rationale, and risks visible first. Inventory projection, vendor offers, budget calculation, and applied preferences use accessible native expandable sections. Dates, quantities, percentages, and currency are human-readable while exact source values remain available as metadata. The existing queued, running, manual-review, safe-error, and read-only boundaries remain unchanged. The focused test first failed because the recommendation-summary landmark did not exist; after implementation, all 18 React tests passed. `npm run lint`, the TypeScript/Vite production build, and `git diff --check` also passed. | Dev image publication, Argo `Synced`/`Healthy`, `make smoke-dev`, and desktop/narrow live browser inspection remain pending. Backend behavior, API contracts, approval actions, and guided explainability timeline remain out of scope. |

### T22 live reconciliation note — 2026-08-14

The recreated dev cluster proved that the External Secrets network repair is
effective: the namespace controller reached the Kubernetes API and remained
`1/1 Running` without the earlier `10.96.0.1:443` timeout. Its next live error
was a namespace RBAC denial while listing
`generators.external-secrets.io/generatorstates`. The minimal declarative
repair grants only `get`, `list`, and `watch` on that resource; it does not add
generator CRUD or cluster-scoped access. Both red-green environment cases, all
16 observability collector tests, Ruff format/check, and all 55 Kubernetes
tests passed. Strict Kubeconform validation again reported 78 valid, 0 invalid,
0 errors, and 7 intentionally skipped External Secrets resources for each
85-resource overlay. Live readiness remains pending the dev Argo application
reconciling this commit.

The next live bootstrap attempt proved the temporary exact-secret IAM policy
and Argo Job recreation path, then failed before database or AWS access because
Python found no writable temporary directory. The Job retained a read-only root
filesystem but, unlike the Odoo Deployment, mounted no `/tmp`. A red-green
render contract now requires the Job to mount a bounded 64 MiB `emptyDir` at
`/tmp`. Both focused environment cases and all 13 application-overlay tests
passed. The complete 55-test Kubernetes suite passed, and strict Kubeconform
validation reported 78 valid, 0 invalid, 0 errors, and 7 intentionally skipped
External Secrets resources for each 85-resource overlay. Live acceptance still
requires the corrected Job to complete, the Odoo API key to synchronize, MCP
to become ready, and the temporary IAM policy to be detached.

Live acceptance then confirmed the corrected bootstrap Job completed in 10
seconds, all six ExternalSecrets synchronized, MCP rolled out `1/1 Running`,
and Terraform removed exactly the temporary IAM attachment and policy with `0`
additions, `0` changes, and `2` destructions. Argo remained `Progressing`
because Pod Security Admission rejected Fluent Bit's required read-only
`/var/log` host mount: the dedicated log namespace enforced `baseline`, which
forbids every `hostPath`. The minimal repair changes only the dedicated
`stockai-logs-*` namespace to `enforce=privileged`; its audit and warning levels
remain `baseline`, the application namespace remains `restricted`, and the
collector retains its non-root, read-only, capability-dropped security context.
The red-green environment cases, all 16 collector tests, and the complete 55
Kubernetes tests passed. Strict Kubeconform validation again reported 78 valid,
0 invalid, 0 errors, and 7 intentionally skipped External Secrets resources per
85-resource overlay. Live acceptance still requires Fluent Bit to become Ready
and Argo to report `Synced` and `Healthy`.

Final live acceptance passed after Argo reconciled that repair: the dev Fluent
Bit pod reached `1/1 Running` with zero restarts, and the `stockai-dev`
Application reported `Synced` and `Healthy`. This completes T22. T23 and T24
have not started.

## Evidence policy

Only commands actually run successfully are recorded as passing. Generated
reports remain local under `reports/` and are not committed.
